{
  "name": "@aurora-enterprise-solutions/aur-module-auth",
  "version": "0.0.1",
  "description": "Aurora Enterprise Solutions Authentication Module",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "start:dev": "npx nodemon",
    "start": "npm run dist && node dist/index.js",
    "build": "rimraf ./dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "eslint": "^8.23.0",
    "nodemon": "^2.0.20",
    "rimraf": "^4.0.4",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.2"
  },
  "dependencies": {
    "@prisma/client": "^4.8.1"
  }
}